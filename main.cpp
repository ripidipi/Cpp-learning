//#define _USE_MATH_DEFINES
#include <iostream>
#include <vector>
#include <chrono>
#include <string>
#include <algorithm>
#include <numeric>
#include <map>
#include <set>
#include <cmath>
#include <random>
#include <unordered_map>
#include <stack>
#include <fstream>
#include <queue>
typedef long double ld;
typedef long long ll;
typedef unsigned int uint;
#define all(a) a.begin(), a.end()
#define null nullptr
#define Min(a) *min_element(all(a))
#define Max(a) *max_element(all(a))
#define Fin ios_base::sync_with_stdio(false); cin.tie(null); cout.tie(0); cout.precision(40);
using namespace std;

const double eps = 1e-7;
const int INF = 1e9 + 9;
const int SIZE = 1e5 + 6;
const int MOD = 1e9 + 7;
const int BASE = 256;

inline ostream& operator << (ostream& out, vector<int>& a) {
    for (auto i: a) {
        out << i << ' ';
    }
    return out;
}

inline istream& operator >> (istream& in, pair<int, int>& a) {
    in >> a.first >> a.second;
    return in;
}

int main() {
    Fin

    return 0;
}


//34
//80 15 12
//120 20 14
//60 22 10
//90 18 14
